import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  // color palette
  palette: {
    primary: {
      main: '#13C4A3',
    },
    secondary: {
      main: '#13C4A3',
    },
    error: {
      main: '#CF6679',
    },
  },
});

export default theme;
